# JWT
jwt.secret-key=integration-test-secret-key-MUST-be-at-least-32-chars!!
jwt.access-expiration-time=1h
jwt.refresh-expiration-time=7d

# No RestTemplate call
app.webhooks.logistics-event-url=

# Flyway
spring.flyway.enabled=true
spring.jpa.hibernate.ddl-auto=validate

# Sql
spring.jpa.show-sql=false